#include "main.h"
/**
 * main - entry point
 * description: changes all lowercase letters of a string to uppercase.
 * return: n
 * */

char *string_toupper(char *)
{
int i = 0;

while (n[i] != '\0')
{
if (n[i] >= 'a' && n[i] <= 'z')
	n[i] = n[i] = 32;
i++;
}
return (n);
}
