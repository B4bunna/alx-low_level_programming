#include "main.h"
#include <stdio.h>
/**
 * main - a function that computes the absolute value of an integer.
 * always return asolute value
 */

int _abs(int)
{

	int i;

	if (i > 0)
	{
	return (i);
	}
	else
	{
	return (i * -1);
	}
}
